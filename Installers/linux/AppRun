#!/bin/bash
# AppImage AppRun entry point for PUCE
HERE="$(dirname "$(readlink -f "${0}")")"
export LD_LIBRARY_PATH="${HERE}/usr/lib:${LD_LIBRARY_PATH}"
export PUCE_PLUGINS_DIR="${HERE}/usr/plugins"

# Execute the main Flutter/Rust compiled binary
exec "${HERE}/usr/bin/PUCE" "$@"
